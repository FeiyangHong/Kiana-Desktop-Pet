# 任务通知接口 v1（预留）

本版只提供进程内 C# 接口、通知合并和示例入口，没有连接 VS Code、下载器或其他软件，也没有 HTTP 服务、开放端口、外部文件监听或 CLI 通知命令。飞书功能未加入。

用户可从「设置 → 使用说明 → 测试一条示例通知」查看效果。右键「通知中心」可查看历史、按来源/状态/未读筛选、标为已读、静音单任务或整个来源；有本地未读时，铃铛打开通知中心。ChatGPT 任务通过「打开 ChatGPT 原生通知」查看。本地已读不会清除 ChatGPT 通知。

## 供未来适配器调用的接口

源码位于 `source/TaskNotices.cs`：

```csharp
ICompanionTaskSink sink = pet.TaskNotices;
sink.Publish(new TaskNotice {
    Source = "示例构建器",
    TaskId = "project-a-build-42",
    Title = "构建项目 A",
    Status = "running"
});

// 同一来源、同一任务，更新其状态。
sink.Publish(new TaskNotice {
    Source = "示例构建器",
    TaskId = "project-a-build-42",
    Title = "构建项目 A",
    Status = "succeeded"
});
```

可以从后台工作线程调用；通知中心内部同步访问，桌宠在自己的 UI 线程更新界面。适配器只应上报真实任务变化，不应假造成功或失败。

## 数据约定

`task-notice.schema.json` 描述预留的 JSON 数据格式。它是未来传输适配器的约定，不是当前已经可调用的网络接口。

```json
{
  "Source": "示例构建器",
  "TaskId": "project-a-build-42",
  "Title": "构建项目 A",
  "Status": "succeeded"
}
```

- `Source`：来源标识，1–60 字符。
- `TaskId`：该来源内稳定的任务标识，1–128 字符；不能每次更新都换 ID。
- `Title`：简短任务标题，1–160 字符。
- `Status`：`running` 进行中、`succeeded` 完成、`failed` 失败、`waiting` 等待处理。
- 时间和未读标记由桌宠生成，调用方不指定。

相同来源和 ID 的重复标题/状态不会再次提醒；状态或标题变化会更新同一条记录并标为未读。最多保留最近 50 项、24 小时内记录；只保存在内存中，退出清空。

本接口没有命令、脚本或任意 URL 执行字段。免打扰时仍可更新铃铛，但不弹气泡；关闭本地任务通知开关后不显示本地角标。已有 ChatGPT 通知行为独立保留。

## 0.6.0：预留显式任务跳转

通知可额外携带 `ActionId`（1–80 字符）。仅受信任的进程内适配器可以为自己的来源注册处理器：

```csharp
pet.TaskNotices.RegisterAction("示例构建器", "show-build", notice => {
    // 由未来适配器实现打开已验证的对应任务入口。
    // 当前版本不提供真实软件连接；这里只是注册方式示例。
});
pet.TaskNotices.Publish(new TaskNotice {
    Source = "示例构建器", TaskId = "project-a-build-42",
    Title = "构建项目 A", Status = "succeeded", ActionId = "show-build"
});
// 适配器停用时：
pet.TaskNotices.UnregisterAction("示例构建器", "show-build");
```

只有用户点击“打开任务”才调用处理器；收到通知、注册处理器和鼠标悬停不会执行。来源与动作标识共同匹配，未注册时按钮禁用。处理器在 UI 点击线程执行，应自行安排耗时工作、验证目标并处理必要的授权。处理器失败时保留未读，处理过程中到来的新任务状态也保留未读。

静音单任务仍保留历史和后续状态，退出清空；来源静音在偏好中保存。静音或已读后的排队气泡会被取消。相同来源和任务的 `ActionId` 变化也视为更新。

以后决定接入某个软件时，再实现该软件的适配器和必要的传输机制，并单独提供启用开关。不要将“预留”误当作当前已自动接收外部事件。
