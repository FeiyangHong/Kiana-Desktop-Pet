# 0.7.7 · 手机通知式横幅与外观设置

- 通知速览默认改为横向细长布局：420 逻辑像素宽、单条任务，快捷操作放在右侧，完整通知中心入口放在底栏。
- 设置“通知中心”新增浮窗宽度、展示条数、紧凑横幅和悬停展开开关；调整自动保存，可随通用偏好导出/导入。
- 长标题省略并提供全文提示；按屏幕工作区限制宽度，保留深浅主题、断线提示和高 DPI 字体绘制。

# 0.7.6 · 仓库更新入口与通知速览

- 设置“场景与维护”增加拉取更新、构建本机修改及仓库绑定；根目录提供对应中文 CMD 脚本，不硬编码盘符或用户目录。
- 拉取仅允许快进，未提交修改、历史分叉、重复部署或构建失败时停止；先构建校验再退出桌宠，备份程序并保留本机偏好。脚本不自动提交或推送。
- 铃铛悬停或首次点击先打开轻量通知浮窗；支持任务跳转、本地已读、完整通知中心入口、外部点击及 Esc 收起。
- 速览保留状态色、更新时间与断线标记；卡片原位刷新，透明窗口只对背景绘制阴影，不缩放文字；支持减少动态效果和深浅主题。
- 跨盘迁移快捷方式时按字节校验备份，兼容旧入口的 EFS 加密属性。

# 0.7.5 · QQ 托盘菜单、E 盘运行与仓库整理

- 识别 QQ 使用的窄型置顶 Chromium 托盘菜单，并按窗口 DPI 缩放尺寸阈值；150% 缩放下完整展开后仍持续让层。实际 QQ 菜单前台采样 110 次，桌宠覆盖次数为 0。
- 安装器支持将桌宠运行目录指定到 E 盘，ChatGPT 平滑联动组件使用同级目录；桌宠从该目录读取 Mini 会话并启动联动组件。
- 保留当前版本的可编译源码、角色素材及测试，将历史截图和一次性校验记录从当前仓库目录移除；原始绘制图集中到 artwork，演示图集中到 docs/demo。
- README 保留效果演示、安装方法和功能说明；完整版本记录在本文件。
# 0.7.4 · 统一程序版本信息

- 偏好包导出和设置页读取统一版本号，不再显示旧版 0.7.1。
- 编译和打包步骤可从 Git 仓库重新生成完整安装包。

# 0.7.3 · 分开显示 Mini 与通知来源

- Mini 已连接但当前任务只从主窗口读取时，设置明确显示 Mini 正常、通知使用备用来源。
- 保留新版 Mini 活动卡片识别及原有的主窗口备用任务来源。

# 0.7.2 · 适配新版 ChatGPT Mini

- 支持新版 Mini 的活动卡片状态。旧宠物状态标记消失时，仍可识别 Mini 已就绪及运行、等待、失败和完成任务。
- 有活动卡片时从 Mini 本身同步数量；打开通知时展开活动面板，避免误触“隐藏活动”。
- 不改变 ChatGPT 程序或会话；原有主窗口备用任务来源继续保留。

# 0.7.1 · 效果预览与联动体验

- 设置新增“效果预览”，模拟蓝/绿/灰/橙角标及空闲状态，按当前偏好试播气泡、倒计时并支持一次试听；不写入真实通知。
- 联动详情分别呈现聊天、语音、通知来源可用性与最后确认时间；持续异常只提醒一次，稳定恢复后才重新计数。
- 通知按状态分组，局部更新卡片；保留滚动、筛选和键盘焦点，缺失更新时间明确标注。
- 导入先预览逐项差异，默认保留本机启动、快捷键、应用名单、联动与坐标；旧包未包含的字段不覆盖现值。
- 通用偏好导出与完整偏好备份分开，携带版本/导出时间；导入确认前不写设置，应用前自动备份。
- 迁移仍仅保留一个桌面入口，旧偏好保持；没有增加自动云同步或软件联动。

# 0.7.0 · 启动、通知与显示恢复

- 设置增加“启动与联动”，ChatGPT 与网易云音乐分别开关，默认不启动其他应用。
- 分别呈现 Mini 和主窗口状态；备用来源明确标记，检查修复针对实际问题，不重启无关应用。
- 铃铛打开统一任务列表，显示状态与更新时间；支持完成提醒时长、声音和静默，保留并标注短断线状态。
- 统一字体、圆角、按钮及工具提示样式，统一工具栏与音乐栏淡入淡出节奏，保留 DPI 清晰度设置。
- 显示与 DPI 变化、解锁和唤醒后分阶段修复位置与层级，尊重手动隐藏和托盘菜单优先规则。
- 旧偏好保留，迁移仍只有一个桌面入口；安装器自动备份旧程序与设置。

# 0.6.8 · 单一桌面入口

- 桌面只保留“琪亚娜桌宠”，旧辅助入口按实际目标核验后备份移走。
- 音乐连接改为从桌宠直接启动，不再依赖桌面快捷方式。
- 安装和迁移不再创建单独的音乐 / 平滑采样桌面图标。
- ChatGPT 和音乐连接入口保留在桌宠设置、右键菜单中。

# 0.6.7 · ChatGPT 消息状态恢复

- 修复 Mini 已关闭但旧页面仍在时，被误判为联动正常的问题；后台模式自动恢复窗口。
- 原 Mini 角标缺失时，使用主窗口已加载本机任务的状态与未读标记补充蓝 / 绿 / 橙 / 红通知。
- 备用通知可从铃铛任务列表打开；已读后收起，无轮询点击，不伪造完成，不重复累计原生角标。
- 保留 0.6.6 的菜单让层与恢复行为。设置、素材和快捷键偏好保持。

# 0.6.6 · 关闭菜单后的稳定恢复

- 将宠物、音乐栏层级切换合并为一次原生批处理，避免分开切换 WPF 置顶属性。
- 按菜单所在层级直接排在其后，减少宠物短暂落到普通应用后面的机会。
- 缩短关闭后的额外等待，并复核实际置顶标记，自动修复漏恢复。
- 排除 DWM 已隐藏的面板及没有实际窗口的残留菜单模式。
- 连续 8 次面板开合、模拟置顶丢失、可见性 / 坐标 / 焦点回归测试通过。

# 0.6.5 · 临时降低层级与托盘面板避让

- 按最新反馈，将菜单打开时直接隐藏改为临时取消置顶，保持可见状态和位置。
- 同时让宠物、气泡、工具栏及音乐栏排在外部菜单后面，关闭后恢复置顶且不抢焦点。
- 新增 Windows 10 / 11 托盘隐藏图标展开面板检测，不需要右键即可让位。
- 子菜单和面板接续操作时持续让位；保留手动隐藏、全屏和免打扰规则。
- 设置开关改名为“托盘面板和外部菜单优先显示”，沿用原有选择。

# 0.6.4 · 外部菜单优先

- 默认在外部菜单打开时临时隐藏宠物、气泡、工具栏和音乐栏，关闭后恢复原位。
- 标准菜单事件与窗口可见性联合检测；托盘右键后识别附近新出现的自定义弹出窗口。
- 子菜单关闭不会提前恢复；菜单进程异常退出后可自动恢复。
- 避让期间暂停走动与置顶修复；保留手动隐藏、全屏、锁屏和免打扰状态。
- 排除桌宠自己的菜单，外观页提供开关，支持偏好迁移与撤销。

# 0.6.3 · 工具栏跟色与音乐尺寸

- 工具栏新增可选跟随音乐配色，复用纯色 / 渐变样式和浓度；连接失效恢复主题底色。
- 音乐栏整体缩放 80%–150%、横向宽度 200–420 可独立调节，保持清晰排版、歌词和按钮避让。
- 右键主菜单缩为九项，五组二级菜单；撤销与键盘操作保留在设置中。
- 修复前台 ChatGPT 同为置顶窗口时盖住桌宠的问题，恢复层级时不抢焦点；真正全屏仍隐藏。
- 全屏规则区分最大化、可见外框与客户区，避免跨屏少量边框溢出误判。
- 保留旧偏好，新增选项随偏好迁移；更新前仍备份，可回退。

# 0.6.2 · 音乐栏封面渐变

- 封面配色增加“封面双色渐变”和“封面 → 主题背景渐变”，保留原有柔和纯色。
- 共用配色开关与程度滑块，支持浅色、深色主题和切歌颜色过渡。
- 设置页实时预览；无封面时示意预览明确标注，实际卡片渐变回退为主题中性色。
- 限制底色强度，并调整渐变模式的次要文字前景色以保持可读性。
- 样式随偏好迁移，支持撤销；旧偏好仍使用原有纯色。

# 0.6.1 · 常驻布局与动作大小

- 音乐栏常驻与工具栏常驻可分别设置，也可从右键菜单切换。
- 常驻音乐栏在工具栏隐藏时贴近宠物，工具栏出现时平滑让位；悬停模式保留。
- 自由拖动坐标独立保存，临时避让不覆盖手动位置；工具栏收起后恢复。
- 鼠标移入常驻音乐栏不再连带展开工具栏，常驻本身不暂停自由走动。
- 三套 Q 版后补的 42 张姿态统一显示比例和接地基线，跟随宠物大小缩放。
- 修正时砾逐光伸懒腰终态的头身比例，保留明亮眼神、透明背景与高清绘制。

# 0.6.0 · 八项操作与视觉优化

- 设置改为左侧分类，增加搜索、常用操作、最近调整；重复开关同步。
- 设置变化和清除布局记忆支持 90 秒内撤销，连续滑块调整合并。
- 自动走动避让鼠标，操作工具栏后稍作停留，保留同向两次后优先反向。
- 音乐长标题悬停展开，暂停卡片可开关，切歌隔离旧封面。
- 本地任务通知中心支持历史、来源/状态/未读筛选和静音；预留显式点击的任务跳转动作。
- 备份和缓存占用管理、保留份数、稳定版保护、清理预览和确认；不会自动清理。
- 局部键盘导航、减少动态效果、大触控区域、DPI 拖动阈值。
- 三套 Q 版新增共 12 张 384×416 透明过渡帧，连接整理头发与伸懒腰动作。

保留用户原有偏好。任务中心不改变 ChatGPT 原生已读状态，没有新增全局快捷键、飞书或 AI 对话。物理触屏及混合 DPI 多屏尚需实际设备验证。

# 0.5.0 · 场景、外观与维护

- 场景方案：工作、休闲、游戏，可保存或覆盖自定义方案；右键快速切换。
- 按显示器组合、分辨率、工作区和 DPI 记住宠物与音乐栏位置，最多 20 套，本机独立保存。
- 通知合并窗口、普通完成静默、隐藏或免打扰结束摘要；不修改 ChatGPT 原生已读状态。
- 隐藏、休息、空闲时减少动画刷新，断线逐渐降低重试频率；已连接 ChatGPT 仍每两秒续期。
- 完整 ZIP 更新预览、完整性校验、自动备份、程序回退和偏好单独恢复。
- 故障诊断预览与导出，默认隐藏个人路径、不包含日志，不自动发送。
- 浅色、深色、跟随系统；工具栏可单独调至 75%–150%。
- 音乐栏封面取色开关和 0–100 程度，默认 20，柔和过渡，保留可读文字与按钮。
- 三套 Q 版新增整理头发、伸懒腰两组动作，共 12 张 384×416 透明绘制帧；眨眼复用既有帧。
- 空闲动作避免连续重复，连续触摸短暂防抖；普通三套复用原有动作。

本版没有加入飞书、独立 AI 对话、自动云同步或自动下载安装。多屏和混合 DPI 使用模拟规则验证，未完成真实多屏插拔实测。性能检查验证调度频率，没有宣称固定比例的省电或 CPU 降幅。
